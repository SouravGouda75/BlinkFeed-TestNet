import React, { useEffect, useState } from 'react'

const SocketPeerVideo = () => {
	const [streams, setStreams] = useState([])
	const [myId, setMyId] = useState()
	const [user,setUser]=useState([])

		return (
				<div>
					Eork
				</div>
		)
}

export default SocketPeerVideo